VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Contraseña de la base (Settings → Database). Solo para scripts locales de migración.
# SUPABASE_DB_PASSWORD=
# Solo en servidor (Vercel Environment Variables) y en local (.env).
# Nunca uses prefijo VITE_ para esta clave (no debe llegar al navegador).
# Tras cambiarla en local: reinicia `npm run dev`.
OPENAI_API_KEY=
